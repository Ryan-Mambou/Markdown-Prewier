import React from "react";
import { FaFreeCodeCamp } from "react-icons/fa";
import style from './box.module.css'
import 'bootstrap/dist/css/bootstrap.min.css';
import ReactMarkdown from 'react-markdown'


/** 
function Previewer({preview}){
    return (
        <div className='d-flex flex-column container bg-primary' className={style.previewer} >
        <div className="row-md p-3 text-center " className={style.topPreviewer}><FaFreeCodeCamp title='yeahhhhhh' /> Previewer</div>
        <ReactMarkdown children={preview} className="row-md bg-danger overflowy-scroll" className={style.bottomPreviewer}>
            </ReactMarkdown>
        </div>
    );
}

export default Previewer; */